@model IEnumerable<FrontEnd.Models.TiposMascotasViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoTipoMascota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreTipoMascota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoTipoMascota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoMascota)
            </td>
            <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.CodigoTipoMascota }) |
                    @Html.ActionLink("Details", "Details", new { id = item.CodigoTipoMascota }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.CodigoTipoMascota })
            </td>
        </tr>
}
    </tbody>
</table>
