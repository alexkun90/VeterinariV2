@model FrontEnd.Models.TiposMascotasViewModel

@{
    ViewData["Title"] = "Crear Tipo de Mascota";
    <link rel="stylesheet" href="~/css/HomeSections/SectionCitas.css" asp-append-version="true" />
}

<section class="section-citas container mt-5">
    <div class="section-header d-flex justify-content-between align-items-center mb-4">
        <h1 class="section-titulo text-primary">
            <i class="fa fa-paw"></i> Crear Tipo de Mascota
        </h1>
    </div>

    <div class="card shadow-lg rounded border-0">
        <div class="card-body p-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger rounded shadow-sm"></div>

                <div class="form-group mb-3">
                    <label asp-for="NombreTipoMascota" class="form-label fw-bold">Nombre del Tipo de Mascota</label>
                    <input asp-for="NombreTipoMascota" class="form-control rounded-pill" placeholder="Ingrese el nombre del tipo de mascota" />
                    <span asp-validation-for="NombreTipoMascota" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between align-items-center mt-4">
                    <a href="@Url.Action("Index")" class="btn btn-outline-secondary rounded-pill px-4">
                        <i class="fa fa-arrow-left"></i> Volver a la Lista
                    </a>
                    <input type="submit" value="Crear" class="btn btn-primary rounded-pill px-4" />
                </div>
            </form>
        </div>
    </div>
    <br />
</section>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
